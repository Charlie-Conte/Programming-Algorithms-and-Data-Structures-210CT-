package week7;

import main.Manager.Tasks;


public class task1 implements Tasks 
{
	@Override
	public void run()
	{
		Graph test = new Graph();
		
		test.addNode(3);
		test.addNode(13);
		
		test.addEdge(0, 1);
		
		System.out.println("id: " + test.GetNode(0).id + " value:" + test.GetNode(0).value);
	}
	
	

}
