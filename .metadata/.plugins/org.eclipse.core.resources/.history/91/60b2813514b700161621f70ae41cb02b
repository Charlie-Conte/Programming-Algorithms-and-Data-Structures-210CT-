package week5;

import java.util.ArrayList;
import java.util.Collections;
import java.util.Comparator;
import java.util.function.Function;

import main.Manager.Tasks;

public class task1 implements Tasks, Comparator 
{
	@Override
	
	public void run() 
	{
		int i = 3;
		int[] n1 = new int[i];
		ArrayList<int[]> holder = new ArrayList<int[]>();
		holder.add(n1);
		//Collections.sort(holder);
		//holder.sort(Function(a));
		
		System.out.println("Rerun Code  y / n");
		String choice2 = main.Manager.read.next();
		if (choice2.equals("y")) run();	
		
		
	}

	@Override
	public int compare(Object o1, Object o2) 
	{
		// TODO Auto-generated method stub
		return 0;
	}
}